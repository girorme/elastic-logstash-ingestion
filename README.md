# elastic-logstash-ingestion
Article material

### Run
- Configure `pipeline/pipeline.conf` with imap credentials and run with `docker-compose up`
- Wait seconds and `gmail_inbox` will be created in elastic
